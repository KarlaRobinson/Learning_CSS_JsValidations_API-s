

<h1>Hi, <%=current_user.name%>!</h1>
<p>Email: <%=current_user.email%></p>
<br>
<p>Aren't you glad you have a personal profile on this website </p>

<br>
<br>
<br>
  <h3>Create a Url</h3>

  <form action="/user_urls" method="post" >
      <input type="text" placeholder="url" name="long_url">
      <input type="submit" value="Go!">
  </form>

<br>
<fieldset>
  <legend>These are your sites below:</legend>
  <br>
  <% sorted_list = @urls.sort_by &:id %>
  <% sorted_list.each_with_index do |object, i| %>
    <a id="<%=object.id%>" href= "/<%=object.short_url%>" > <%=i+1%>. <%=object.short_url%>   Visted (<%=object.click_count%>) times.</a>
  <br>
  <% end %>
  <br>
</fieldset>
<br>
<br>
<fieldset>
  <legend>Sign out</legend>
  <form action="/sign_out" method="post">
    <input type="submit" value="Exit this account.">
  </form>
</fieldset>